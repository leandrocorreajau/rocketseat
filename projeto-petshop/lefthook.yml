pre-commit:
  commands:
    check:
      run: yarn format {staged_files}
      glob: '*.{js,jsx,ts,tsx,css,json,md}'
      stage_fixed: true

pre-push:
  commands:
    validate:
      glob: '*.{ts,tsx}'
      run: yarn validate:typecheck
